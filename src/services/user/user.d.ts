declare namespace UserTyping {
  interface UserInfo {
    id: number;
    name: string;
    email: string;
    website: string;
  }

  interface UserDTO {
    name: string;
    email: string;
    website: string;
  }
}